# Local Gene Sets

Place versioned local GMT files here.

Each GMT source should have metadata:

```text
source
version
release_date
species
identifier_type
checksum
license_or_usage_note
```

The first implementation does not require internet access and does not silently remap gene IDs.
